{
  services.mako = {
    enable = true;
    settings = {
      border-radius = 10;
      padding = 10;
      # By default, notifications disappear after 5s
      default-timeout = 5000;
      # Make icons smaller
      max-icon-size = 32;
    };
  };
}
