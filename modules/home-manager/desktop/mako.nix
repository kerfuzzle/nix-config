{
  services.mako = {
    enable = true;
    settings = {
      border-radius = 10;
      padding = 10;
      # By default, notifications do not disappear until dismissed
      default-timeout = 0;
    };
  };
}
